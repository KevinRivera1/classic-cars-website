---
interface Props {
  name?: string;
  class?: string;
}

const { name }: Props = Astro.props;
---

<button
  class={`outline cursor-pointer hover:bg-amber-400 active:bg-amber-400 outline-[#27262d] px-6 py-2 rounded-lg ${Astro.props.class || ""}`}
>
  {name || "Button"}
</button>
